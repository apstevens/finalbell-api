name: Deploy to Production

on:
  push:
    branches: [master, main]
  workflow_dispatch: # Allow manual trigger

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci

      - name: Generate Prisma Client
        run: npx prisma generate

      - name: Build application
        run: npm run build

      - name: Build Docker image
        run: docker build -t final-bell-api:${{ github.sha }} .

      - name: Tag as latest
        run: docker tag final-bell-api:${{ github.sha }} final-bell-api:latest

      # Example: Push to Docker registry
      # Uncomment and configure for your registry (Docker Hub, ECR, GCR, etc.)
      # - name: Login to Docker Hub
      #   uses: docker/login-action@v3
      #   with:
      #     username: ${{ secrets.DOCKER_USERNAME }}
      #     password: ${{ secrets.DOCKER_PASSWORD }}
      #
      # - name: Push to Docker Hub
      #   run: |
      #     docker tag final-bell-api:latest ${{ secrets.DOCKER_USERNAME }}/final-bell-api:latest
      #     docker push ${{ secrets.DOCKER_USERNAME }}/final-bell-api:latest

      # Example: Deploy to your server via SSH
      # Uncomment and configure for your deployment method
      # - name: Deploy to server
      #   uses: appleboy/ssh-action@v1.0.0
      #   with:
      #     host: ${{ secrets.DEPLOY_HOST }}
      #     username: ${{ secrets.DEPLOY_USER }}
      #     key: ${{ secrets.DEPLOY_SSH_KEY }}
      #     script: |
      #       cd /opt/final-bell-api
      #       docker-compose pull
      #       docker-compose up -d
      #       docker system prune -f

      - name: Notify deployment success
        if: success()
        run: echo "Deployment successful!"

      - name: Notify deployment failure
        if: failure()
        run: echo "Deployment failed!"
